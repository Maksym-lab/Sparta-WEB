import styled from 'styled-components';
const Grid = styled.div`
  display: grid;
  grid-template-rows: repeat(1, 1fr);
  column-gap: 8px;
  row-gap: 5px;
  margin-top: -40px;
  padding: 0 15px;
`;
export default Grid;
